# 财务口径说明

本说明由财务团队维护，用于对账与结算。与运营口径存在已知差异，对账前请先确认
使用哪一套。

## 新增用户

新增用户按 `users.first_order_at` 的首单日期计数：用户产生第一笔成交才算获客。

从未下单的注册用户不计入本口径。

## 成交额

成交额为净额：`status = 'paid'` 的订单金额，扣除 `status = 'refunded'` 的退款金额。

退款按原订单日期冲减，不计入退款发生当期。
