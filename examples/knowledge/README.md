# 合成电商口径文档（演示与验收用）

这些文件是**虚构的**，用来演示 QueryAgent 的核心场景：同一个业务词在不同
团队的文档里有不同取法，而两边都写得像是权威定义。它们对应
`examples/demo_ecommerce/demo_shop.db` 的表结构。

`ops/` 与 `finance/` 归属两个不同的业务空间。默认演示身份在 `ops`，因此
检索不到 `finance/` 的内容——这是权限演示的一部分，不是配置遗漏。

**文档只描述规则，不写死数字。** 演示数据的日期相对今天生成，任何写进文档的
具体数值都会在第二天变成错的。
