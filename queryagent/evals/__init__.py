"""Evaluation toolkit: cases, result comparison, runner, public-benchmark tools.

Code lives inside the package (instead of a repo-root ``eval/`` module) so
``queryagent eval`` works after ``pip install``; the *data* (cases.yaml,
public subset, reports) stays in the repo-root ``eval/`` directory.
"""
