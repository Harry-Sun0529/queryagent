---
label: wayfinder:map
effort: v0.5.0
---

# v0.5.0 — 让声明与证据对齐

## Destination

一个 v0.5.0 tag，使这个项目**README 里的每一句声明都有对应证据**，并把
评测里统计力最弱的两个指标补到能说话的样本量。四轮审计后遗留的短板清单
清零（或明确记为 out of scope）。

## Notes

- **本 effort 把 execution 纳入 map**（覆盖 plan-don't-do 默认值）。
- 遵循 skills：`tdd`（预声明 seam、红→绿、一次一个纵切）、`code-review`。
- **铁基线**：自建集 自修正后 ≥17/18 · 追问全对。任一回退即回滚。
- **本轮的指导教训**（来自 v0.4.0 推送后 CI 变红）：**本地绿 ≠ CI 绿 ≠
  真的能用**。任何"我们支持 X"的声明，必须指出证据在哪；拿不到证据时，
  改声明，而不是留着让读者默认。
- 唯一可用模型：DeepSeek。Anthropic 无 key，因此 A2 的终点是**诚实标注**
  而非"验证通过"。

## Decisions so far

<!-- one line per closed ticket -->

- [T19 — 给「库」一个真正的公开 API](tickets/19-public-api.md) — `__all__`
  + README 库用法段，且该示例由测试实际执行。
- [T20 — 版本号单一来源与依赖上界](tickets/20-version-and-pins.md) —
  `__version__` 读包元数据；全部依赖加上界。

## Not yet specified

- B1 补足样本后，追问/口径指标若显著低于现在的 4/4，README 叙事要不要调整。
- 并行 eval 若引入结果不稳定（并发下的限流/重试差异），是否值得保留。
- 公开 API 定型后，是否需要为它单独写 semver 承诺（目前 semver 只覆盖 CLI
  与 config）。

## Out of scope

- **embedding 口径匹配**（ADR-002 明确预留为未来实现；本轮是"对齐声明"，
  不是加能力）。
- PyPI 上传、跨厂商模型对比（无 key）。
- `serve` HTTP 层、会话内口径粘性（沿用前几轮边界）。
