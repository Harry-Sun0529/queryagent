# 接续断点（2026-09-09，可靠性修复与真机对照里程碑结束）

## 当前授权与边界

用户要求先对每个问题写修复/验收计划，再实施，避免冗余防御代码。已授权本地修复、真实 DeepSeek 调用及启动 Docker；未授权推送，本轮不提交/不推送。用户用量已刷新，随后要求在最近里程碑停下并输出接力文档，以便新窗口继续。当前已停止，不再调用模型或开发。

## 仓库/Git

`/Users/sunweizhuo/Desktop/queryagent`，main 基线 d00b6c7 v0.5.0，远端先前确认相同且CI成功。所有本轮更改均未提交，包括新文件；不要清理或重置。先读 `git status`。

## 必读文件

- `docs/specs/reliability-repair-2026-09.md`：修复计划（每项方法/验收）。
- `eval/README.md`：新版指标与断点行为。
- `docs/wayfinder/tickets/26-parallel-live-equivalence.md`：历史遗留票据，现已按修订后的真机证据目标关闭，不代表统计等价证明。
- `/Users/sunweizhuo/Documents/Playground/queryagent-audit-2026-09-09.md`：原审计证据。

## 已完成修复

1. JSONL以bytes逐条解析，UTF-8残片不毁整文件；续写前隔离未换行尾部；真实续跑拒绝缺失/旧签名。
2. 新 `queryagent/evals/identity.py`：题目/配置/metrics/SQLite及WAL/日期/代码内容指纹；服务端续跑需初始就指定 `--data-version`，密码不入日志。
3. 并发：最多N个在途任务、完成即落盘、最终排序；熔断停止提交但保存在途结果；Ctrl-C恢复日志并排空在途结果后返回130。
4. MySQL：超时设置失败停止查询，I/O和池等待有界；重连失败恢复计数；SSCursor截断丢弃连接避免drain；None队列信号唤醒容量等待者，复用同一deadline。
5. 评测v2：保留SQL轨迹命中，另报正常完成且命中；不声称自然语言答案正确；首次SQL不受后续失败影响；截断结果不算完整匹配；逐题显示completed。
6. README/SECURITY/eval文档及统计ADR收紧过强声明；CHANGELOG只追加Unreleased，不改历史数字。
7. 独立review已验证池等待修复和Ctrl-C/逐题报告修复，无新具体问题。

## 已验证

最新完整运行：ruff通过、mypy32源文件通过、pytest **305 passed, 1 skipped**（唯一skip上游BIRD原题文件缺失）。Docker已启动，MySQL/ClickHouse容器均运行，真实集成通过。MySQL `SELECT SUM(SLEEP(0.001)) FROM users` 被服务端约1秒终止，后续请求恢复；截断后下次查询正常。

## 网络中断暴露的问题已修复

CaseResult增加retryable布尔值；run_case在原异常存在时分类，OutageGuard传给
UpstreamOutage，不再用RuntimeError包装后的文本猜测类型。新测试
`test_eval_network_outage_preserves_retryability`红→绿，连接失败返回75；401/402保持2。

## 凭据与真实评测

用户新提供专用DeepSeek key，已授权随用并报告额度问题。仅保存于仓库外 `/Users/sunweizhuo/.config/queryagent/deepseek.env`，权限0600；不要复制密钥进本文件、日志或Git。当前2026-09-09检查余额¥49.08、available=true。模型列表API确认有deepseek-v4-flash/pro。

- `eval/results/reliability-2026-09-09/`：初次selfbuilt36完整，SQL轨迹25/28，正常完成且命中24/28，追问16/16；总33/36含clarify，不可混淆分母。另serial-1是因复核修复而人工中断的5条记录，不是完整基线。
- `eval/results/reliability-2026-09-09-final/`：serial-1仅2条完成，然后5次ConnectError，manifest退出2；这不是余额不足，也不是完整性能样本。
- **正式评测已全部完成**：`eval/results/reliability-2026-09-09-rerun/README.md` 是最终报告。
- `manifest.json`保存4轮完整墙钟、退出码、token/费用；四轮各100题、无缺测、同签名、源码哈希未变。
- S1/P1/P2/S2：SQL命中43/43/47/44，命中且完成43/42/46/42；串行24–26分钟，并行约9分钟，均值比2.73×。
- 第二次并行原为检验重复运行波动；用户提出停下时，经检查后台已完成四轮。没有人工中止本次完整轮。
- 启动器原PID31074已退出，pid文件只是历史记录；已核查无launcher/eval进程。不再启动新调用。
- 正式脚本 `eval/run_concurrency_check.py` 需新的输出目录；原始日志和报告保留，不要覆盖。
- 自建集补跑未做；先前selfbuilt36是稍早代码版本，不能声称为最新代码完整回归结果。

## 当前停靠里程碑与后续

修复已验收，四轮真实对照已归档，T26按修订后的证据目标关闭；未证明统计等价。
用户要求在此停止，以新窗口继续。当前无新功能开发、无运行中评测、未提交/推送。

1. 新窗口先读修复计划、正式结果报告及git diff（包括untracked），无需重做已通过检查。
2. selfbuilt36较历史低的原因尚未受控归因；是否补跑最新代码，应在新窗口决定。不要为抬分改题/改prompt，不碰封存test200。
3. 若继续产品迭代，依据下方约定先写每项实现/验收计划，再做核心CLI确认闭环；不要直接跳到UI/MCP装饰层。
4. 准备提交前检查未跟踪证据文件与运行log/pid哪些应纳入；不能把当前dirty main误当作已发布版本。

## 下轮产品范围（本轮不实施）

RAG检索内部文档并对比口径冲突；每次都展示产品/运营能读懂的口径并显式确认。可复用历史选择但不是团队标准；缺规则允许用户补充并标明来源。维护者配置字段映射。身份由可信应用传入，文档/历史按权限过滤，数据库用只读账户/受限视图。确认前不查业务数据；总查询次数/时间/并发预算由维护者配置不可被模型或用户提高。MCP用于嵌入其他Agent；Web简约对话+独立确认单，开源设计样本待自行研究。按核心CLI闭环→MCP/Web→场景评测及答辩三阶段。

额外已确认细节：首个文档provider覆盖Markdown、DOCX及可提取文本的PDF；预留企业知识库connector，不在首版承诺全部供应商。
建立合成电商文档与口径冲突场景作演示/验收。探索必须只读且有总预算，行数上限不等于扫描上限。
用户还不能独立讲清核心调用链，后续应安排结合真实代码的讲解与答辩，不把功能堆叠当作简历合格标准。
UI设计参考需要届时自行搜索筛选开源样本；本轮未做参考调研。

## 续：切片 1A 已完成（2026-09-09 同日）

阶段0 核对通过（基线 305 passed / 1 skipped，无后台评测）。随后完成阶段1 切片1A
「持久化草案与强制确认门」，详见 `docs/specs/workflow-slice-1a-2026-09.md`
（含计划、不变量 I1–I10、验收结果与明确不承诺的部分）。

当前断点：`queryagent flow` 可从 CLI 演示「口径冲突 → 用户选择 → 显式确认 →
只读执行 → 带口径回述的结果」，未确认/改版后旧确认/无映射/跨主体四条路径均被拦截。
RAG（1B）、计划编译与通俗解释（1C）、历史沿用（1D）、统一预算（1E）尚未开始。
本轮及此前所有修改仍未提交、未推送。
